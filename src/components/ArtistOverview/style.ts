import Theme from '../../theme';

// eslint-disable-next-line import/prefer-default-export
export const BaseComponentStyle:any = {
  maxWidth: 400,
  width: 'calc(100% - 20px)',
  display: 'inline-block',
  verticalAlign: 'top',
  margin: Theme.content.margin,
  textAlign: 'left',
};
